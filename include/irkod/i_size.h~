#ifndef IRKOD_SIZE_I_H
#define IRKOD_SIZE_I_H

#include <stddef.h>
#include <irkod/thing/thing.h>

IRKOD_THING_DECLARE_I_ID(irkod_size);

struct irkod_size_i
{
	size_t (*get)(const struct irkod_thing *it);
};

#endif /* !IRKOD_SIZE_I_H */
