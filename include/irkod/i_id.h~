#ifndef IRKOD_ID_I_H
#define IRKOD_ID_I_H

#include <irkod/thing/thing.h>

IRKOD_THING_DECLARE_I_ID(irkod_id);

struct irkod_id_i
{
	void (*set)(struct irkod_thing *it, const char *id);
	const char *(*get)(struct irkod_thing *it);
};

#endif /* !IRKOD_ID_I_H */
